"""
Discord Bot Entry Point

This is the main entry point for the Discord bot when running in 
a normal Python environment rather than directly in Replit.
It simply imports and runs the app module.
"""

from app import start_server

if __name__ == "__main__":
    start_server()